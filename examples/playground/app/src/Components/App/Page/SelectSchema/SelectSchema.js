/** import bootstrap */
import React, { Component } from 'react';
import { Container } from 'reactstrap';

export default class SelectSchema extends Component {

    /** render view */
    render = () => {
        return (
            <Container>
                SCHEMA SELECT
            </Container>
        )
    }
}